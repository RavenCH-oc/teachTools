export function TrueFalseEditor({ value, onChange }: { value: boolean; onChange: (value: boolean) => void }) { return <fieldset className="answer-control"><legend>正確答案</legend><label><input checked={value} name="true-false-answer" onChange={() => onChange(true)} type="radio" /> 正確</label><label><input checked={!value} name="true-false-answer" onChange={() => onChange(false)} type="radio" /> 錯誤</label></fieldset>; }
